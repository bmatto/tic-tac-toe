import React, { PropTypes } from 'react'
import styles from './board.scss'

const propTypes = {

}

export default function Begin(props) {
  return <button className={styles.begin}>Begin</button>
}

Begin.propTypes = propTypes;
